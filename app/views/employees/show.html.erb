<% if current_user && current_user.admin_role? %>
    <%= render partial: "partials/show_structure", locals: {model_name: "employee", 
                                                            object: @employee,
                                                            primary: "employee_data", 
                                                            secondary: nil,
                                                            sidebar: "assign_store"} %>
<% else %>
    <%= render partial: "partials/show_structure", locals: {model_name: "employee", 
                                                            object: @employee,
                                                            primary: "employee_data", 
                                                            secondary: nil,
                                                            sidebar: nil} %>
<% end %>             