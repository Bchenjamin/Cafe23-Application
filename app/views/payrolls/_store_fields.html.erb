<% if current_user && current_user.admin_role? %>
    <%= f.collection_select :store_id, Store.alphabetical.active.to_a, :id, :name, prompt: true %>
<% end %>
<%= f.date_field :start_date %>
<%= f.date_field :end_date %>